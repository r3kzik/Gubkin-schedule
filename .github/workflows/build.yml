name: Собрать APK (Lite)

on:
  push:
    branches: [lite]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v4

      - name: Сборка
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Переименовать APK
        run: cp app/build/outputs/apk/release/app-release.apk raspisanie-lite.apk

      - name: Сохранить APK в сборке
        uses: actions/upload-artifact@v4
        with:
          name: raspisanie-lite-apk
          path: raspisanie-lite.apk

      - name: Опубликовать релиз (удобно скачивать с телефона)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: lite-${{ github.run_number }}
          name: Расписание Lite 1.0.${{ github.run_number }}
          body: Лёгкая версия без эффектов. Ставится рядом с основным приложением. Скачайте raspisanie-lite.apk ниже.
          files: raspisanie-lite.apk
          make_latest: false
